"""Задача 1 (оператор if)
Напиши код, который проверяет, что переменная age больше 18, и выводит "Доступ разрешен"."""

age = 19
if age > 18:
    print("Доступ разрешен")

"""Задача 2 (оператор else)
Напиши код, который:

Проверяет, что переменная temperature меньше 0.

Если да — выводит "Лёд".

Если нет — выводит "Вода".

Пример переменной:

temperature = -5"""

temperature = -5
if temperature < 0:
    print("Лёд")
else:
    print("Вода")


"""Задача 3 (оператор elif)
Дана переменная:

time = 14  
Напиши код, который проверяет:

Если time < 12 — выводит "Утро",

Если 12 ≤ time < 18 — выводит "День",

Иначе — "Вечер"."""

time = 14
if time < 12:
    print("Утро")
elif 12 <= time < 18:
    print("День")
else:
    print("Вечер")

"""Задача 4 (логические операции)
Даны две переменные:

is_rainy = True  
is_windy = False  
Напиши условие, которое выводит "Зонт нужен", если:

Идёт дождь (is_rainy) ИЛИ ветрено (is_windy),

Но только если при этом is_windy не равен True (используй not).

Подсказка: Комбинируй or и not."""

is_rainy = True
is_windy = False

if not is_windy and (is_rainy or is_windy == True):
    print("Зонт нужен")

"""1. Оператор if
Напиши код, который проверяет, является ли число x = 10 положительным. Если да — выводи строку "Число положительное".

(Используй только if, без else/elif.)"""

x = 10
if x > 0:
    print("Число положительное")

"""2. Оператор else
Напиши код, который проверяет, чётное ли число y = 7. Если да — выводи "Чётное", иначе — "Нечётное".

(Используй if и else.)"""

y = 7
if y % 2 == 0:
    print("Чётное")
else:
    print("Нечётное")

"""3. Оператор elif
Дана переменная:

time = 14
Напиши код, который выведет:

"Утро" — если time от 5 до 11,

"День" — если от 12 до 17,

"Вечер" — если от 18 до 22,

"Ночь" — в остальных случаях."""

time = 14
if 5 <= time <= 11:
    print("Утро")
elif 12 <= time <= 17:
    print("День")
elif 18 <= time <= 22:
    print("Вечер")
else:
    print("Ночь")

"""4. Логические операции
Даны две переменные:

is_rainy = True
is_windy = False
Напиши код, который выведет "Возьми зонт", если идет дождь И нет ветра, иначе — "Сегодня без зонта"."""

is_rainy = True
is_windy = False

if is_rainy and not is_windy:
    print("Возьми зонт")
else:
    print("Сегодня без зонта")

"""5. Тернарный оператор
Дана переменная:

age = 18
Напиши одну строку кода, которая:

Проверит, что age >= 18,

Если да — выведет "Доступ разрешён",

Иначе — "Доступ запрещён".

(Используй тернарный оператор x if condition else y.)"""

age = 18
result = "Доступ разрешён" if age >= 18 else "Доступ запрещён"
print(result)

"""6. Логические операции в условиях
Даны три переменные:

has_ticket = True
has_passport = False
has_vip = True
Напиши код, который выведет "Проход разрешён", если:

У человека есть билет И паспорт (но если есть VIP-статус, паспорт не требуется).

Иначе — "Проход запрещён".

(Используй скобки для группировки условий.)"""

has_ticket = True
has_passport = False
has_vip = True

if (has_ticket and has_passport) or has_vip:
    print("Проход разрешён")
else:
    print("Проход запрещён")

"""7. Вложенные условия (if внутри if)
Даны переменные:

temperature = 25
is_summer = True
Напиши код, который выведет:

"Жарко" — если temperature > 30 летом (is_summer = True),

"Тепло" — если temperature > 20 (в любое время года),

"Прохладно" — в остальных случаях."""

temperature = 25
is_summer = True

if temperature > 30 and is_summer:
    print("Жарко")
elif temperature > 20:
    print("Тепло")
else:
    print("Прохладно")

"""8. Комбинированные условия
Даны переменные:

is_guest = False
is_admin = True
has_payment = True
Напиши код, который выведет "Доступ открыт", если:

Пользователь админ ИЛИ

Он не гость И есть оплата.

Иначе — "Доступ закрыт"."""

is_guest = False
is_admin = True
has_payment = True

if is_admin or (not is_guest and has_payment):
    print("Доступ открыт")
else:
    print("Доступ закрыт")

#Задача 1.
#Напиши код, который проверяет, является ли число x = 10 чётным. Если да — выведи строку "Чётное".

#Используй только if (без else/elif).

x = 10

if x % 2 == 0:
    print("Чётное")

#Задача 2.
#Даны два условия:
#a = True
#b = False
#Напиши код, который выводит "Оба истинны", только если a и b равны True.

a = True
b = False

if a and b == True:
    print("Оба истинны")

#Задача 3.
#Дано число:
#num = 7
#Напиши одну строку кода, которая присваивает переменной result строку "Больше 5",
# если num > 5, иначе — "Меньше или равно 5".

num = 7

if num > 5:
    result = f"Больше 5"
else:
    result = f"Меньше или равно 5"

print(result)

num = 7
result = "Больше 5" if num > 5 else "Меньше или равно 5"
print(result)

#Задача 4.
#Напиши программу, которая проверяет число temperature = 23 и выводит:
#"Жарко", если temperature > 30
#"Тепло", если temperature между 20 и 30 включительно
#"Прохладно", если temperature < 20

temperature = 23

if temperature > 30:
    print("Жарко")

elif 20 <= temperature <= 30:
    print("Тепло")

else:
    print("Прохладно")


#Задача 5.
#Даны три переменные:
#x = 5
#y = 10
#z = 15
#Напиши условие, которое выводит "Да", если:
#x меньше y И одновременно y меньше z.

x = 5
y = 10
z = 15

if x < y and y < z:
    print("Да")

#Задача 6.
#Дано число:
#number = 4
#Напиши код, который:
#Проверяет, является ли число чётным (выводит "Чётное"),
#Иначе выводит "Нечётное".

number = 4

if number % 2 == 0:
    print("Чётное")

else:
    print("Нечётное")


#Задача 7.
#Даны два условия:
#is_rain = False
#is_wind = True
#Напиши код, который выводит "Возьми зонт", если:
#Идёт дождь (is_rain = True) ИЛИ дует ветер (is_wind = True),
#Иначе выводит "Зонт не нужен".

is_rain = False
is_wind = True

if is_rain or is_wind:
    print("Возьми зонт")

else:
    print("Зонт не нужен")

#Задача 8.
#Дано число:
#age = 18
#Напиши одну строку кода, которая:
#Присваивает переменной access строку "Доступ разрешён", если age >= 18,
#Иначе — "Доступ запрещён".

age = 18
access = "Доступ разрешён" if age >= 18 else "Доступ запрещён"


#Задача 9.
#Даны три переменные:
#a = True
#b = False
#c = True
#Напиши условие, которое выводит "Сработало", если:
#a и c равны True, но b равно False.

a = True
b = False
c = True

if not b and (a and c):
    print("Сработало")

#Задача 10.
#Дано число:
#score = 85
#Напиши программу, которая выводит оценку:
#"Отлично", если score >= 90,
#"Хорошо", если score между 70 и 89,
#"Удовлетворительно", если score между 50 и 69,
#"Неуд" во всех остальных случаях.

score = 85

if score >= 90:
    print("Отлично")

elif 70 <= score <= 89:
    print("Хорошо")

elif 50 <= score <= 69:
    print("Удовлетворительно")

else:
    print("Неуд")

#Задача 11.
#Даны три переменные:
#has_ticket = True
#has_passport = False
#has_vaccine = True
#Напиши условие, которое выводит "Можно лететь", если:
#У человека есть билет (has_ticket) И
#Либо есть паспорт (has_passport), либо есть вакцина (has_vaccine) (хотя бы что-то одно).

has_ticket = True
has_passport = False
has_vaccine = True

result = "Можно лететь" if has_ticket and (has_passport or has_vaccine) else "Сиди дома"

print(result)

#Задача 12.
#Дано число:
#x = 12
#Напиши код, который:
#Сначала проверяет, является ли число положительным (> 0),
#Если да, затем проверяет:
#"Чётное", если делится на 2 без остатка,
#"Нечётное", если нет.
#Если число не положительное, выводит "Не положительное".

x = 12

if x > 0:
    if x % 2 == 0:
        print("Чётное")

    else:
        print("Нечётное")

else:
    print("Не положительное")